# Frontend (King Vibez)

Install dependencies:
```bash
cd frontend
npm install
```

Run dev server:
```bash
npm run dev
```

By default the frontend will request API at http://localhost:5000 (see VITE_API_URL)
